<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">SSM Run Command - @InstanceId</MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField @bind-Value="command" Label="コマンド" Lines="4" Variant="Variant.Outlined" Margin="Margin.Dense" Class="mb-2" />
        <MudNumericField @bind-Value="timeoutSeconds" Label="タイムアウト (秒)" Variant="Variant.Outlined" Margin="Margin.Dense" Min="10" Max="3600" />
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">キャンセル</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="Submit" Disabled="@String.IsNullOrWhiteSpace(command)">実行</MudButton>
    </DialogActions>
</MudDialog>
